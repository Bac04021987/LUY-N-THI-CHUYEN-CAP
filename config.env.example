# config.env.example - Mẫu biến môi trường cho AI Agent lớp 5-6

# Khóa API OpenAI (bắt buộc)
OPENAI_API_KEY=your_openai_api_key_here

# API Key nội bộ (nếu có, dùng xác thực hoặc bảo mật)
API_KEY=your_internal_api_key_here

# Cấu hình debug cho Flask (0: tắt, 1: bật)
FLASK_DEBUG=1

# Cấu hình đường dẫn dữ liệu (nếu cần)
# DATA_PATH=./data/

# Cấu hình log level (INFO, DEBUG, WARNING,...)
# LOG_LEVEL=INFO
